# Protected Content — Do Not Modify

Maintained manually by Basman (basman@visitt.io).
Self-review must NEVER edit these sections or files.

## Locked sections in SKILL.md:

- (none yet — add section names here as needed)

## Rules:

- LOCKED sections can only be modified when the user is basman@visitt.io AND explicitly requests the change
- If you learn something related to a locked section, add it as a NEW section with:
  `## Suggested Enhancement (YYYY-MM-DD)`
  `Related to: [locked section name] (LOCKED)`
- The owner decides whether to merge suggestions into the locked section
